import { test } from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { join, dirname } from "node:path";
import { fileURLToPath } from "node:url";
import { createRequire } from "node:module";

// P6 코어 크기 상한(one-in-one-out) — 연구 F5(컨텍스트 활용률)·BMAD project-context lean 규칙 근거.
// 상시 주입 규칙의 팽창은 곧 모든 세션 비용 + 중간 규칙 희석(안전 캡슐 존재 이유). 이 테스트는 상한을
// 기계로 강제한다: 넘기려면 (a) 다른 규칙을 줄이거나(one-in-one-out), (b) 아래 상한 상수를 같은 커밋에서
// **현재 크기로 다시 박는다** — 후자는 한 줄 가시 diff라 리뷰어가 "코어가 변했다"를 반드시 본다.
// v0.49.0 후속부터 이 재핀은 **양방향**이다(커졌을 때만이 아니라 줄었을 때도 — 아래 BAND_BYTES).
// 계측 아님(정적 파일 크기 검사, 로컬 로깅·카운터 없음).
// 재현성: 개행을 LF로 정규화해 재므로 OS/checkout(CRLF)에 무관(+ .gitattributes eol=lf 이중 방어).
const ROOT = join(dirname(fileURLToPath(import.meta.url)), "..");
const RULES_DIR = join(ROOT, "src", "rules");
function normBytes(p) {
  return Buffer.byteLength(readFileSync(p, "utf8").replace(/\r\n/g, "\n"), "utf8");
}

// v0.64.1: 재는 대상을 **한 파일 → 주입되는 규칙 소스 전체 합**으로 넓혔다. 안 그러면 조각 사이로
// 글을 옮기거나 부록에만 규칙을 넣어 이 예산 검사를 빠져나가는 길이 남는다(아래 한계 3번이 자백해 둔 구멍).
// **대상 목록은 손으로 적지 않는다** — activate-core.APPENDICES 에서 뽑아 코어 파일과 합친다.
// 그래야 부록을 새로 만들면서 예산 검사를 빠져나가는 길이 없다(부록 등록 자체는
// test/rule-pieces.test.mjs 가 src/rules 파일 목록과 양방향으로 강제한다).
// 🛑 문자 검사(test/rule-pieces.test.mjs)와 이 바이트 검사는 **다른 축**이고 서로를 대신하지 못한다.
//    문자 = 조각 하나가 CLI 문턱을 넘어 잘리는 것(물리 천장·외부 사실).
//    바이트 = 상시 주입 비용이 소리 없이 부는 것(예산·정책). 합치면 안 된다.
// 머리말은 예산에 안 넣는다 — 규칙 내용이 아니라 코드가 만드는 문자열이라, 조각 수가 바뀔 때마다
// 장부가 흔들리면 장부의 뜻이 흐려진다. 머리말까지 포함한 길이는 문자 검사가 잰다.
const require = createRequire(import.meta.url);
const INJECTED = ["operating-rules.md", ...require("../src/hooks/activate-core.js").APPENDICES.map((a) => a.file)];
function injectedBytes() {
  return INJECTED.reduce((sum, f) => sum + normBytes(join(RULES_DIR, f)), 0);
}

// 🛑 합계 핀만으로는 **파일 사이 이동**을 못 본다(pr-reviewer 1차 medium):
//    부록에서 1,000B 빼서 코어에 넣으면 합계가 같아 장부가 침묵한다. 두 파일은 성격이 다르다 —
//    코어는 **모든 세션에 항상** 드는 비용이고 부록은 **무인 세션에만 가끔** 드는 비용이다.
//    합계 하나로 재면 "항상 드는 비용"이 소리 없이 커지는 것을 그 축이 못 본다. 그래서 파일별로도 핀다.
//    아래 FILE_CEILINGS 의 합 = CEILING_BYTES 라는 단언이 두 장부가 따로 놀지 못하게 묶는다.
const FILE_CEILINGS = {
  "operating-rules.md": 20686,     // v0.67.0 T7-가 실측(헤드룸 0 · 사유는 아래 장부 맨 끝)
  "unattended-appendix.md": 5912,  // v0.67.0 T7-가 실측(헤드룸 0)
  "statusboard-appendix.md": 698,  // v0.65.0 실측(등록부에 처음 오름 · T7 에서 무변경 = 긴 줄표 0개)
};

// Claude 상시 주입 = operating-rules.md 단독. batch6 다이어트 + batch7 영어화 기준으로 하향.
// 2026-07-27 +140 (17500→17640): "접기"(맞지만 사소한 low를 한 줄로 묶어 보고) 규칙. v0.38.0이 게이트의
// 억제 지시를 없애 "전부 올려라"로 바꿨는데, 그 정당화였던 "메인이 걸러준다"의 필터가 실제로는 없었다
// (수신 규율은 틀린 발견의 기각만 정의). Fable 독립 감사 medium. 분량 압박에 안전 단서를 깎지 않기로 해
// 상향(plan-validator CONDITIONAL 조건 4): 반드시 남길 5개 = medium 이상 금지 · 개수+종류 표기 ·
// 애매하면 접지 않기 · 접기는 기각 아님(요청 시 전체) · 대조 확인 면제 아님.
// 2026-08-07 +309 (17640→17949): 게이트 재검증 회차의 **쓰기 규칙** 신설(v0.46.0).
// 초안 +187에 pr-reviewer 반영분 +122: 마커를 번역하지 말 것(영어 세션에서 조용히 꺼짐) ·
// 지적 제목은 직전 회차 것만(누적하면 계획서가 커져 다음 검증기가 새 지적을 내는 되먹임을 규칙이 먹임) ·
// **통과하면 마커를 지운다**(GO 난 계획서를 이어 쓸 때 남은 옛 숫자가 새 작업의 첫 검증을 N차로 만들고,
// "모르면 붙인다"와 겹쳐 없는 루프에 표시·finding이 붙는다 — pr-reviewer medium).
// v0.42.0이 넣은 수렴 가드는 읽는 쪽(plan-validator)만 있었고, 그 회차를 적으라는 규칙이
// src 어디에도 없었다(grep 실측) — 아무도 안 적으면 게이트는 영구히 1차로 보고 규칙이 한 번도 안 켜진다.
// 스킬 위임 검토: 재검증은 routing·formats가 안 떠 있는 자리에서 일어나고, PreToolUse 리마인더는
// 세션당 1회성(pretooluse-core.js가 첫 게이트 후 validated를 굳힘)이라 상시 바닥 외에 도달 경로가 없다.
// 이 변경분 단독 델타다(작업 트리에 다른 미커밋 변경 0건인 상태에서 측정 — plan-validator 4차 H-3).
// 2026-08-07 +252 (17949→18201): **코드 재리뷰** 회차의 쓰기 규칙 신설(v0.48.0). 바로 앞 문장
// (plan-validator 재검증)과 어순을 맞춘 한 문장이다. 담은 것 넷 = verbatim(영어 세션에서 번역돼
// 조용히 꺼지는 것 방지) · 마커 `재리뷰 회차: N` · 지난 회차 blocker/high 제목(그 회차만) ·
// 계수 기준과 범위(count every review, first = 1 / a different change restarts at 1).
// 뒤 둘이 없으면 쓰는 쪽과 읽는 쪽이 한 칸 어긋난다(plan-validator 3차 H-2) — 읽는 쪽은 "모든 리뷰를
// 세고 최초가 1차"로 세는데 쓰는 쪽이 "고쳐 다시 낸 것만" 세면 넘기는 숫자가 다르다.
// **왜 스킬이 아니라 코어인가**: finish-check는 저발동 실측이 있고(honclwd 제외 10개 프로젝트 중 1회),
// routing은 지연로드라 코어보다 발동 신뢰도가 낮다. 이 규칙은 v0.42.0 실패("읽는 쪽만 있어 2,941회 중 0회 발동")의 처방이라 가장 확실한 자리에 둔다.
// **왜 릴레이(리뷰어 출력 꼬리로 다음 회차를 넘김)가 아닌가** (plan-validator 2차 H-2): 도착 시점이
// N회차 끝인데 행동 시점은 N+1회차 시작이라 그 사이 수정 사이클·압축·세션 교체를 지나야 하고,
// 이전 리뷰어의 숫자를 베끼는 홉 체인이라 한 홉만 끊기면 영구히 1로 돌아가며 복구 경로가 없다.
// plan-validator와 조건이 다른 이유: 계획 검증은 회차 소스 3개 중 2개가 계획서 파일에 앵커되지만
// 코드 리뷰의 대상은 diff라 그 2개가 통째로 없다 — 남는 건 호출 프롬프트뿐이다.
// 이 변경분 단독 델타다(작업 트리에 다른 미커밋 변경 0건인 상태에서 측정).
// ⚠ 규칙을 스킬로 옮겨 이 상한을 피하는 변경은 발동률 근거를 커밋 메시지에 남긴다.
// v0.49.0에 Codex 인라인 총면 상한이 사라졌다. 그 자리는 아래 BAND_BYTES가 **절반만** 대신한다:
// 코어를 줄여 옮기는 경로에는 마찰이 생기지만, **처음부터 스킬에만 넣는 경로에는 여전히 마찰이 0**이다
// (받는 쪽에 크기 핀이 없다 — finish-check 저발동 실측 참조).
// 2026-08-07 재핀 18201 → 18136: **규칙 내용 변경 없음.** 아래 BAND_BYTES(64) 도입에 따른 정확 핀이다.
// 그동안 누적된 여유 65바이트를 흡수한 것이지 규칙을 지운 것이 아니다 — diff만 보면 감축으로 읽히니
// 이 줄이 그 오해를 막는다. 이후 델타는 18136 기준으로 계산한다.
// 2026-08-08 +1760 (18136 → 19896): **출력 스타일 절 신설**(v0.51.0) +1790, `:3` 머리말 설명 −30.
// (+83은 pr-reviewer 1차 medium 2건: 길이 면제에 `한눈에 · 🙋 확인 필요` 추가 —
// 빠져 있으면 '짧게' 압박에 대리결정 목록이 잘려 그 게이트의 존재 이유가 꺼진다 ·
// `Words` 줄에 증거 인용·게이트 라벨 예외 — 없으면 무증거 ✅로 가는 길이 열린다.)
// 2026-08-09 +187 (19896 → 20083): **비전문가 요약의 모양 변경**(사용자 요청 2026-08-09
// "요약이 꼭 이렇게 길어야 하나 · 그래서 다음에 내가 뭘 해야 하는데?"). 세 가지가 들어갔다:
// (1) 칸마다 한 줄, (2) 잘되면은 무엇/왜에서 자명하면 생략, (3) 다음에 확인할 것을 **닫는 칸**으로
// 올리고 그 내용을 "사용자가 다음에 할 일(명령형·1~2개)"로 못박음 — 기존 정의는 "개발자에게
// 요청하거나 직접 테스트할 것"이라 내가 이어서 할 일과 섞여 사용자가 자기 몫을 못 골라냈다.
// **왜 스킬(chageun:formats)이 아니라 코어인가**: 상세(작성 예시·칸 뜻)는 실제로 formats 로 넣었고
// 코어에는 **모양을 바꾸는 최소 문장만** 남겼다. formats 는 지연 로드라, 그 스킬이 안 뜬 턴의 요약은
// 코어 골격만 보고 쓰인다 — 거기에 "한 줄씩·닫는 칸은 사용자 몫"이 없으면 옛 모양으로 되돌아간다.
// (+91 추가분) 같은 날 후속 지적: 한 칸에 항목이 여럿일 때 가운뎃점으로 이어 붙여 줄이 다시 길어졌다.
// "항목마다 한 줄, 여럿이면 번호로 쪼갠다"를 코어에 넣는다 — 이게 없으면 칸 수만 줄고 줄 길이는
// 그대로라 짧게 쓰는 목적이 무너진다(사용자가 실제 요약을 보고 다시 지적한 사례).
// 사용자 전역 지침(`~/.claude/CLAUDE.md`)에만 있던 답변 방식 규칙을 코어로 옮긴 것이다 — 그 전에는
// 이 사용자의 차근과 다른 사용자의 차근이 답변 길이·어휘·표기에서 달랐다(README `:34`가 "쉬운 말로
// 설명"을 공개 약속하는데 코어 뒷받침은 `:7` 한 줄뿐이었다).
// ⚠ **역대 최대 증가다.** 장부에 오른 규칙 1건의 크기는 140·309·252였고 이번은 그 5배가 넘는다.
// 크기의 대부분은 plan-validator 1차가 요구한 안전 문장이다: 의례 면제(FULL 카드·요약·끝 점검은
// 10줄 기준에서 뺀다) · "짧게가 안전 정보를 지우라는 뜻이 아니다" · 카드 우선 단서.
// 회수 예정 502B는 **LIGHT/FULL 통합 작업에 의존**한다(아직 안 함 — 그 전까지는 회수 없음).
// 502의 근거 = 작업 규모 스위치 절의 LIGHT 렌더링 2줄(`LIGHT renderings —`부터 `unaffected.`까지)
// 실측치다. 통합하면 그 두 줄이 통째로 사라진다.
// 스킬 위임 검토: 이 규칙은 매 응답에 걸리는데 스킬은 지연로드라 저발동 실측이 있다
// (finish-check: honclwd 제외 10개 프로젝트 중 1회). 상시 바닥 말고는 도달 경로가 없다.
// 적용 범위 정직 고지: SessionStart는 **본 세션에만** 주입되므로 plan-validator·pr-reviewer·
// code-implementer 출력에는 이 절이 안 닿는다.
// ⚠ **다음 코어 증가 때는 재핀 전에 실제로 뺀다.** 재핀이 v0.51.0까지 5회 연속이고 상쇄로 줄인
// 적은 0회다(위 장부 전체가 증가 기록뿐이다). 이 검사는 자물쇠가 아니라 도장이라, 이 약속이
// 사람 눈에 띄는 것이 유일한 방어선이다 — 커밋 메시지에 적으면 이 자리에서 안 보인다(pr-reviewer 2차 low).
// 첫 후보 = LIGHT/FULL 통합(위 502B).
// 2026-08-09 (+123, 20083 → 20206) — **그 약속을 이번에 못 지켰다. 6번째 재핀이고 순증이다.**
// 있는 그대로 적는다: 상쇄 후보로 지목해 둔 502B(LIGHT/FULL 통합)는 이번 변경이 오히려 LIGHT
// 렌더링에 닫는 칸을 **더한** 쪽이라, 통합을 지금 하면 이번 사용자 요청과 정면으로 부딪힌다.
// 정확히 적는다: **기존 문장을 지운 것은 없다.** 새로 넣으려던 "라벨을 그대로 써라" 문장이 `:9`의
// "Summary fields, all five exactly … verbatim"과 같은 말이어서 `:9`를 가리키는 조각으로 줄여
// **64B 덜 넣은** 것이다(회수가 아니다 — pr-reviewer 2차 low). 회수 후보를 따로 훑었지만
// 안전 캡슐(`:138`)처럼 **일부러 둔 중복**뿐이라 손대지 않았다.
// 내용 = pr-reviewer 1차 high/medium 반영: 라벨 축약이 finish-work 훅의 요약 감지를 꺼뜨린 것을
// 되돌리고(스킬 쪽 수정이 본체), 번호 규칙의 적용 범위를 `summary only`로 좁히고,
// LIGHT 한 줄에도 닫는 칸 한 조각을 붙였다(사용자가 가장 자주 보는 쪽이 안 고쳐져 있었다).
// (+198 추가분, 20206 → 20404) pr-reviewer 2차: LIGHT 요약에 닫는 칸을 붙이자 판정어가 2개가 되어
// **훅이 LIGHT 턴을 헛되돌릴** 확률이 올라갔다(면제는 본문의 `LIGHT` 글자로만 걸리는데 그걸 찍으라는
// 규칙이 어디에도 없었다). `:21`에 "LIGHT 라고 표시한다"와 닫는 칸 조각을 함께 넣어 기존 면제를
// 살린다 — 훅 코드는 안 건드린다. 오차단은 이 저장소가 v0.42.1 핫픽스까지 낸 최대 실패 양식이다.
// 2026-08-09 +181 (20404 → 20585): **CONDITIONAL 출구 수리**(Fable 독립 심판 최우선 권고).
// 실측 사고에서 게이트가 CONDITIONAL 을 **7번** 냈는데 착수는 0번이었다 — 나흘 중 사흘이 blocker 0
// 상태였다. 원인은 판정을 받고 **게이트를 다시 돌린 것**인데, "재검증은 판정에 대한 답이 아니다"라는
// 문장이 어디에도 없었다. plan-validator (당시 `:98`)의 "보완을 구현 중 해결 가능"과 코어 `:53`
// ("멈춰라")이 서로 반대라 메인 세션이 다시 검증받는 쪽을 골랐다. 게이트 문서도 같이 고쳤다.
// **회수 −97B 를 처음으로 실제로 했다**(3차 리뷰 low C): `:21` 의 훅 동작 설명 괄호를 줄였다(131→34B).
// 같은 이유가 바로 위 장부 항목에 이미 적혀 있어 코어에서 두 번 값을 치르고 있었다.
// 추가분은 `:53` 게이트 판정 불릿의 +278 이고, 278 − 97 = 181 로 실측(20404→20585)과 맞는다.
// ⚠ **숫자 두 개를 손으로 세다 또 틀렸다**(pr-reviewer 1차 medium). 처음 적은 "회수 −110B"와
// "v0.54.0 브랜치 3회 · 이번이 9번째"가 둘 다 어긋났다. 다시 잰 값: 그 브랜치의 재핀은
// **4회**(19896→19992→20083→20206→20404)이고, 상한을 **올린** 재핀은 이번이 **10번째**다
// (17500→17640→17949→18201 · 18136 으로 좁힘 · →19813→19896→19992→20083→20206→20404→20585).
// 세는 법 = `git log -G'^const CEILING_BYTES' -- test/core-size.test.mjs` + 각 커밋의 값 출력.
// 바이트도 `git ls-tree -l <커밋> -- src/rules/operating-rules.md` 로 잰다 — **손으로 세지 않는다.**
// 회수 후보(LIGHT/FULL 통합)는 그 두 줄이 커져 502B → 약 690B 다.
// (+12, 20585 → 20597) pr-reviewer 2차 low: 위 문장을 다시 짜면서 `report conditions **to the user**,
// get approval` 의 "to the user" 를 중복이라 걷어냈는데, 뒤의 "goes to the user" 와 **다른 행동**이다
// (앞=조건을 사용자에게 보고하고 그 승인을 받는다 · 뒤=아무도 안 정하는 루프를 사용자에게 올린다).
// 멈춤 규칙에서 승인 주체가 빠진 것이라 코어 원칙("짧게가 안전 정보를 지우라는 뜻이 아니다")대로 되돌린다.
// 이 12B 는 상쇄 없이 순증이다 — 이번 회차의 회수분(-97)이 이미 다 쓰였다.
// 2026-08-11 +122 (20568 → 20690): 두 줄을 함께 고쳤다.
//   (1) :71 +61 : 안전 tie-break 에서 실행 위치(inline)를 풀고 BLOCKED 계약을 못 박음
//       (사용자 결정: 판단 걸린 일도 백그라운드 위임). 모델 축(top-tier·Never Sonnet)은 무변경.
//   (2) :53 +61 : "hook enforces" 를 "hook enforces edits made in this session;
//       delegated fixes: re-run manually" 로. 위 (1)로 구현이 서브에이전트로 가면 그 편집이
//       메인 기록에 안 남아 신선도 훅이 스스로 안 깨진다. 고치지 않으면 코어가 매 세션
//       사실이 아닌 것을 선언하고, 메인은 기계가 막아 주는 줄 알고 그냥 push 한다.
// 회수 시도: 못 찾았다. 장부가 지목해 둔 유일한 후보인 LIGHT/FULL 통합(`:21`~`:22` 의 LIGHT 렌더링
//   2줄, 오늘 실측 603B)을 열어 봤는데, 통합 자체가 별개 작업이고 이번 변경은 그 두 줄을 건드리지도
//   않는다 — 여기서 빼면 이번 커밋이 범위 밖 규칙 변경을 몰래 태우는 것이 된다. 다른 후보로 안전
//   캡슐(`:133`)도 다시 봤지만 `:89` 가 이미 적어 둔 대로 **일부러 둔 중복**이라 손대지 않았다.
//   즉 `:85` 의 약속을 또 미룬 것이다. 침묵하지 않고 그대로 적는다.
//   상한을 **올린** 재핀은 이번이 12번째다(기준점 17500 — 각 커밋의 CEILING_BYTES 값을 뽑아
//   앞 커밋보다 올라간 것만 셌다. 목록 줄 수를 센 것이 아니다).
// 이 문장들을 코어에 두는 이유(정직 기재): (1)의 실제 수신자는 **서브에이전트**인데 코어는
//   서브에이전트에 안 닿는다(에이전트 본문·라우팅 스킬이 닿는 자리다). 그래도 코어에 두는 것은,
//   **위임할지 말지를 정하는 쪽이 메인 세션**이고 그 판단이 이 한 줄에서 갈리기 때문이다.
//   (2)는 정반대로 **수신자가 메인 세션뿐**이라 코어 말고는 닿을 자리가 없다. push 시점에
//   메인이 읽는 자리가 코어 하나다(5차 HIGH-1 이 잡은 것이 정확히 이 점이다).
//   서브에이전트가 실제로 읽는 계약은 deep-implementer.md·code-implementer.md 가 따로 들고 있다.
// 2026-08-12 재핀 20690 → 26614 (v0.64.1). **규칙은 한 글자도 안 늘었다.**
//   1. 무엇이 바뀌었나: 재는 대상이 "코어 파일 하나" → "주입되는 규칙 소스 전체 합"으로 넓어졌다.
//      20,690(operating-rules.md) + 5,924(unattended-appendix.md) = 26,614. 델타 +5,924 는
//      **새 규칙이 아니라 그동안 아무 핀도 없던 파일이 장부에 처음 올라온 것**이다.
//   2. 왜: 조각 사이로 글을 옮겨 예산 검사를 피하는 길과, 부록에만 규칙을 넣어 검사를 통과하는
//      길을 함께 막는다(아래 한계 3번이 자백해 둔 구멍 — 그 자백을 이번에 반만 메운다.
//      src/skills·src/agents 는 여전히 핀이 없다).
//   3. 다음 델타는 26,614 기준으로 계산한다.
//   회수: 없다. 이번 커밋은 규칙 본문을 안 건드린다(대상 확대뿐).
//   상한을 **올린** 재핀은 이번이 13번째다 — 단 이번 것은 앞 12번과 성격이 다르다(규칙 증가가
//   아니라 측정 범위 확대). 앞으로 세는 사람이 헷갈리지 않게 여기 적어 둔다.
// 2026-08-12 +40 (26614 → 26654): **규칙 삭제·축약 없음.** 자리 참조 3문장을 이름 기반으로
//   다시 쓴 델타다(조각 순서 뒤섞임 대응 — 실측 6회 6회 전부 섞였고, 무인 시행에서는 부록
//   조각이 첫 번째로 도착했다).
//     operating-rules.md 20690 → 20710 (+20): `:133` 제목 "restated at the end" →
//       "restates the other sections" · `:135` "each section above" → "the named section in each line".
//     unattended-appendix.md 5924 → 5944 (+20): `:3` "위 코어" → "운영 규칙 `Stop rules` 절"
//       (메인 세션 결정 — 계획서 §8-5 는 "알고 남긴다"였으나 고치는 쪽으로 뒤집혔다).
//   회수: 없다. 세 문장 다 **다시 쓰기이지 줄이기가 아니다** — 뜻을 그대로 두고 자리 대신 이름으로
//   가리키게 바꿨고, 그 과정에서 이름이 자리 낱말보다 길어 20B씩 늘었다.
// 2026-08-12 재핀 26654 → 27352 (v0.65.0 rebase). **코어 규칙은 한 글자도 안 늘었다**
//   (operating-rules.md 20710 그대로 · unattended-appendix.md 5944 그대로).
//   1. 무엇이 바뀌었나: 상황판 부록(`statusboard-appendix.md` 698B)이 **등록부에 처음 올랐다.**
//      델타 +698 은 새 규칙이 아니라, 그동안 아무 핀도 없던 파일이 장부에 처음 올라온 것이다.
//   2. 왜 지금인가: 이 부록은 feat/v0.65.0 에서 `activate.js` 가 **하드코딩으로** 이어 붙이고
//      있었다. 그 갈래는 실제로 주입되면서 조합 매트릭스에도 이 예산에도 안 잡혔다 —
//      v0.64.1 이 자백해 둔 구멍과 **같은 방향**이다. rebase 때 하드코딩을 지우고
//      `activate-core.APPENDICES` 에 등록했고, 그 순간 INJECTED 가 자동으로 이 파일을 집는다.
//   3. 성격: 규칙 증가가 아니라 **측정 범위 확대**다(v0.64.1 재핀과 같은 갈래).
//      상한을 **올린** 재핀으로는 14번째이고, 그중 규칙 증가가 아닌 것은 이번이 두 번째다.
//   4. 다음 델타는 27352 기준으로 계산한다.
//   회수: 없다. 이번 변경은 규칙 본문을 안 건드린다(등록 + 대상 확대뿐).
//   ⚠ 상시 비용이 그만큼 는 것은 **아니다.** 상황판 부록은 프로젝트 루트에 `status.md` 가
//     있을 때만 주입된다(없으면 `existsSync` 한 번이 전부다). 그래도 장부에 올리는 이유는
//     파일별 핀이 "코어는 모든 세션 · 부록은 조건부"라는 성격 차이를 보게 해 주기 때문이다.
// 2026-08-12 재핀 27352 → 27311 (v0.66.0). **줄인 것이 아니라 옮긴 것이다.**
//   operating-rules.md 20710 → 20669 (−41). 부록 둘은 무변경(unattended 5944 · statusboard 698).
//   줄별 실측 델타: `:75` 제목 −16(괄호 안 소유자 이름만 뺐다 — 기계 키 `Proceeding by task type`
//     (조각 명단)과 `작업 유형별 진행`(rules-labels)은 **글자 그대로** 남겼다. 바꾸면 그 절이
//     조용히 안 실린다) · `:77` −12 · `:78` +24(기획 입구를 `chageun:planning` 강제 포인터 + 내장
//     `Plan` 에이전트로) · `:79` −3(`systematic-debugging` → `chageun:debugging`) ·
//     `:80` −99(`subagent-driven-development` 제거 · `executing-plans` 는 남겼다) ·
//     `:82` +65("없으면 알린다"를 이 흐름의 **모든** 스킬·에이전트로 넓히고, 설치가 불가능한
//     내장 `Plan` 에는 폴백(메인이 직접 쓴다)을 못박았다).
//   🛑 **이 −41 은 회수가 아니라 이동이다.** 코어에서 뺀 41바이트 자리에 지연 로드 스킬 두 벌이
//     새로 들어왔다: `src/skills/planning/SKILL.md` 4,297자 · `src/skills/debugging/SKILL.md` 2,099자
//     (합 6,396자 · 귀속 표시 `debugging/NOTICE` 2,949자는 규칙이 아니라 라이선스 문서라 별도).
//     상시 비용은 41B 줄었지만 **규칙 총량은 늘었고, 늘어난 쪽은 그 스킬이 안 뜬 세션에서 조용히 꺼진다.**
//     ("장부 최초의 순감"이 아니다 — 위 `:136` 이 회수 −97B 를 이미 기록해 뒀다.)
//   아래 한계 3번이 자백해 둔 사각(**처음부터 스킬에만 넣기**)을 이번에 실제로 밟았다. 그래서 받는
//     쪽에는 크기 핀 대신 **문자열 앵커**를 걸었다: test/planning-skill.test.mjs ·
//     test/debugging-skill.test.mjs(**양방향** — "두 번 빗나가면 멈춘다" 존재 + 3회 문투 부재 ·
//     배선 4자리의 옛 이름 부재) · test/rules-labels.test.mjs(`chageun:debugging` 문자열 · 강제 포인터 6문장).
//   발동률 근거(위 ⚠ 가 요구하는 것): 강제 포인터(`load ... via the Skill tool`)는 **기획에만** 붙였다.
//     스킬 저발동 실측(finish-check: honclwd 제외 10개 프로젝트 중 1회)이 있어 새 기능 입구 하나의
//     발동률을 우선했고, 디버깅은 포인터 문투를 안 쓰는 대신 위 문자열 앵커가 그물이다(의도된 비대칭).
//   회수: 없다. 이번 커밋은 그 41B 를 다른 규칙으로 되메우지 않았다.
//   다음 델타는 27311 기준으로 계산한다.
// 2026-08-13 재핀 27311 → 27507 (v0.67.0). **순증 +181 이고, 핀에 헤드룸 15B 가 남아 있다.**
//   부록 둘은 무변경(unattended 5944 · statusboard 698). operating-rules.md 20669 → 20850.
//   🛑 **이 항목의 숫자는 재리뷰 1회차 medium 을 반영해 다시 잰 것이다**(초안은 +196 · 20865 였다).
//     아래 `:82` 되돌리기가 −15 라 밴드(0~64) 안이고, 밴드 안에서는 재핀하지 않는 것이 이 검사의
//     규약이라 핀은 27507(파일별 20865) 그대로 두었다. **그래서 핀 값과 실측이 15B 다르다** —
//     이 저장소 장부에서 그 둘이 어긋난 채 남는 첫 항목이니, 다음 사람이 핀을 실측으로 읽지 않도록
//     여기 적어 둔다. 실측 기준값은 27492(operating-rules.md 20850)다.
//   🛑 이 값은 **작업 트리에서 normBytes 잣대로 잰 것**이고 커밋 뒤 `git ls-tree -l` 로 대조했다.
//     `git ls-tree -l` 을 커밋 전에 쓰면 옛 값이 나오는데, 핀이 틀리면 검사가 잡아도
//     **이 사유 줄의 델타와 방향은 어떤 검사도 안 본다**(사람이 읽는 기록이라 조용히 틀린 채 남는다).
//   줄별 실측 델타: `:79` −4(`test-driven-development` → `chageun:test-design`) ·
//     `:80` −22(`executing-plans` 이름만 뺐다. 남은 것은 `delegation per chageun:routing` 이고
//       새 주인은 그 위임이다 — 실행 중 단계 체크는 계획서 안의 `- [ ]` 토글이다) ·
//     `:82` +61(스킬·에이전트가 없을 때의 처방. 초안은 이것을 **두 갈래로 갈랐다가**(`chageun:*` 는
//       재설치 · 남의 것은 "설치/활성") 재리뷰 1회차 medium 으로 **한 갈래로 되돌렸다**: 179 → 255
//       → 240. 되돌린 이유 = 그 갈래는 대상이 0개였다. 이 흐름 절(`:77`~`:83`)이 부르는 이름은
//       전수 확인 결과 **전부 우리 것**이고(유일한 예외 = 내장 `Plan`), 오히려 접두어를 안 단 우리
//       이름 다섯(`referencing` · `design-system` · plan-validator · pr-reviewer · `:78` 끝의 bare
//       `routing`)이 "남의 것" 갈래로 **오분류**돼 "pr-reviewer 를 따로 설치하라"는 불가능한 안내가
//       나왔다. 막다른 길을 없애려 넣은 +76 이 다른 막다른 길을 만든 셈이라, 갈래를 합치고 15B 를
//       되찾았다. `never fail silent` · `don't route` 두 조각과 `Plan` 폴백 갈래는 그대로 남겼다) ·
//     **`:83` +138(신설 · 우리 스킬이 있으면 남의 스킬을 부르지 않는다)** ·
//     `:131` +8(`(writing-skills)` 예시를 빼고 대상 부류를 skills·rules·hooks·memory 넷으로 적었다.
//       −17 +25 인데, 이 저장소는 실제로 훅과 메모리도 고치는데 그 둘이 승인 규칙 밖이었다).
//   🛑 **한 갈래로 합친 것이 맞는 조건을 못박는다: 이 절이 남의 스킬·에이전트를 하나도 안 부르는
//     동안만 맞다.** 이 절에 남의 이름을 다시 적으면 그 사용자에게는 "차근을 재설치하라"가 틀린
//     처방이 되고, 이번에 지운 "설치/활성" 갈래가 다시 필요해진다. 그 조건을 지키는 그물 =
//     test/rules-labels.test.mjs(코어에 옛 이름 `test-driven-development` 부재 단언 · `chageun:*`
//     포인터 6문장 존재) + test/debugging-skill.test.mjs(`systematic-debugging` 부재, 배선 4자리) +
//     test/test-design-skill.test.mjs(옛 이름 부재, 2자리).
//     🛑 재리뷰 2회차 medium: 위 그물이 이 판이 걷어낸 옛 이름 **넷 중 하나**(test-driven-development)
//     만 막고 있었다 — 나머지 셋(executing-plans · using-git-worktrees · writing-skills)은 재등장을
//     잡는 칸이 하나도 없었다. test/rules-labels.test.mjs 에 두 검사를 더해 채웠다: (a) 코어(RULES)
//     에 셋이 안 남았다 (b) src/rules/*.md + src/skills/*/SKILL.md + src/agents/*.md 전체에 넷이 안
//     남았다 — `using-git-worktrees` 는 코어가 아니라 스킬 본문(routing/SKILL.md)에 있던 이름이라
//     (b)가 있어야 잡힌다. `src/skills/test-design/NOTICE` 는 (b)에서 **일부러 뺐다**: 그 파일
//     `:12`가 `test-driven-development` 를 MIT 저작권 표시로 정당하게 들고 있어(라이선스 고지),
//     잡으면 저작권 표기를 지우라는 잘못된 압력이 된다(SKILL.md 파일명만 지목해 읽으므로 같은 폴더의
//     NOTICE 는 애초에 대상이 아니다).
//     ⚠ **정직 회계: 이 그물도 여전히 부분적이다.** 걷어낸 옛 이름 **넷**의 재등장만 막고, **처음 보는
//     남의 스킬 이름**이 이 절에 새로 적히는 것은 아무 검사도 막지 않는다. 그때 이 갈래를 되살릴지
//     판단하는 것은 사람이다.
//   🛑 **순증 사유를 정직하게 적는다.** 이 판은 수퍼파워스 이름을 걷어내는 판이라 "줄어든다"고
//     예상하기 쉬운데, 실제로는 늘었다(+181). 늘린 자리는 셋이다: `:83` +138(새 규칙 한 줄) ·
//     `:82` +61(걷어낸 이름이 하던 안내를 우리 말로 다시 적은 값 — 초안의 두 갈래 가르기는 되돌렸다) ·
//     `:131` +8(승인 대상 부류를 넷으로 적었다). 줄인 자리는 둘이다: `:79` −4 · `:80` −22.
//     207 − 26 = 181 로 실측(20669 → 20850)과 맞는다. 이름을 빼면 그 이름이 하던 안내(무엇을
//     설치하라 · 어느 스킬을 부르라)가 사라지므로, 그 자리를 우리 말로 다시 적어야 해서 늘어난 것이다.
//   회수: 없다. 이번 medium 반영으로 `:82` 가 15B 줄었지만 그것은 **다른 규칙을 줄여 상쇄한 '회수'가
//     아니라 같은 줄에 잘못 넣은 값을 되돌린 것**이다(장부 `:136` 의 −97B 가 여전히 마지막 회수다).
//   ⚠ **T7(긴 줄표 걷어내기)이 같은 파일에서 회수한다.** 그 걸음의 시작점은 20669 도 20865 도 아니라
//     **20850** 다(핀 20865 가 아니다 — 위 헤드룸 15B). 계획서 6-3 표의 참고값(20,669 기준 −140)은
//     v0.67.0 으로 무효가 됐다. T7 이 −140 을 실제로 회수하면 헤드룸이 155 로 밴드를 넘으니
//     **그 걸음에서는 FILE_CEILINGS 와 CEILING_BYTES 를 함께 재핀해야 한다.**
//   발동률 근거(위 ⚠ 가 요구하는 것): `:79` 가 가리키는 `chageun:test-design` 에는 강제 포인터
//     (`load ... via the Skill tool`)를 **안 붙였다** — 매번 본문을 대화에 실으면 컨텍스트를 줄이려는
//     이 판의 목적과 반대다. 옛 스킬 `test-driven-development` 실측 발동은 197세션 중 4세션이고,
//     대체가 그보다 덜 뜰 수 있다는 것을 받아들였다(과발동 방지 조건도 안 넣은 이유가 같다).
//     그물은 test/test-design-skill.test.mjs(본문·뺀 것·배선 양방향) + test/rules-labels.test.mjs
//     (`chageun:test-design` 존재 + 옛 이름 부재 + '우리 스킬 먼저' 한 줄)다. 배포 뒤 실제 발동은
//     `docs/tools/scan-skill-firing.mjs` 로 **새 세션 20개가 쌓인 다음** 센다(그전에는 분모가 없다).
//   다음 델타는 **실측 27492** 기준으로 계산한다(핀 27507 이 아니다 — 위 헤드룸 15B).
// 2026-08-13 재핀 27507 → 27296 (v0.67.0 T7-가). **회수 −196 이다. 이 판 최초의 순감이고,
//   장부 `:136` 의 −97B 이후 두 번째 회수다**(T3 은 순증 +181 이었으니 이 판에서 방향이 뒤집힌 첫 항목이다).
//   🛑 **핀과 실측의 15B 어긋남이 이번에 사라졌다.** 바로 위 항목이 핀 20865 · 실측 20850 으로
//     갈라진 채 남겨 뒀는데(밴드 안이라 재핀 안 함), 이번 회수가 밴드를 넘겨 재핀을 부르면서
//     둘을 실측으로 다시 맞췄다. 그래서 이 항목의 델타는 **두 기준에서 값이 다르다**:
//     핀 기준 27507 → 27296 = **−211** · 실측 기준 27492 → 27296 = **−196**.
//     **회수로 세는 값은 −196**(실제로 파일에서 사라진 바이트)이고, −211 은 옛 핀의 여유 15B 가
//     함께 정리된 값이다. 두 숫자를 섞어 쓰면 다음 사람이 회수량을 15B 부풀려 읽는다.
//   파일별: operating-rules.md 20850 → 20686 (−164) · unattended-appendix.md 5944 → 5912 (−32) ·
//     statusboard-appendix.md 698 → 698 (무변경 - 이 파일에는 긴 줄표가 처음부터 0개다. 짐작이
//     아니라 세 파일을 다 재서 확인했다).
//   🛑 **무엇을 줄였나: 규칙의 뜻은 한 글자도 안 줄었다.** 긴 줄표(UTF-8 3바이트) 79개를
//     `:`(1바이트) 또는 `-`(1바이트)로 바꾼 것이 전부다. 자리별 대체 46 × `: `(−3) +
//     32 × ` - `(−2) + 1 × 숫자 범위 `1–2`→`1-2`(−2) = **−204**. 규칙 문장을 지우지 않았고
//     새 규칙도 안 더했다.
//   🛑 **늘린 자리 하나(+8)**: `:41` 끝 문장을 사실에 맞게 고쳤다. 옛 문장은
//     "chageun's own source files still contain long dashes; do not copy that style."(77B)로,
//     **금지문과 예시가 같은 파일에 있다는 자백**이었다. T7 이 그 예시를 없애 문장이 거짓이 되므로
//     "Long dashes remaining in chageun's own source are machine-judgment values, not style."(85B)
//     로 바꿨다. 남는 두 자리(`operating-rules.md:41` 금지 목록 자신 · `table-to-yaml.mjs:37`
//     `PRIO_SEEN`)가 **판정 값이라 못 바꾼 자리**임을 밝히는 쪽이 정직하다.
//     −204 + 8 = **−196** 으로 실측(27492 → 27296)과 맞는다.
//   🛑 **`:41` 금지 목록의 네 글자(`—` `–` `―` `ㅡ`)는 안 바꿨다.** 그 목록은 금지할 글자를
//     **값으로** 들고 있어, 같이 치환하면 규칙이 "무엇을 쓰지 말라는 것인지"를 잃는다.
//     그리고 그 사고를 잡는 칸이 하나도 없었다(전수 확인 - 지워도 751개가 전부 초록).
//     그래서 이 걸음에서 앵커를 새로 만들었다: test/rules-labels.test.mjs 의
//     "출력 표기 금지 목록이 금지할 글자를 값으로 들고 있다" - 목록의 **괄호 안쪽만 떼어**
//     글자마다 확인한다("파일에 — 가 있다"로 잡으면 이 한 자리만 남았을 때에도 통과해 우연히
//     맞는 검사가 된다). 부수기 시험 3개로 빨간불을 확인했다(한 글자 삭제 · 세 글자 일괄 치환 ·
//     `avoid` 지시 분리).
//   🛑 이 값은 **작업 트리에서 normBytes 잣대로 잰 것**이고 커밋 뒤 `git ls-tree -l` 로 대조했다.
//     `git ls-tree -l` 을 커밋 전에 쓰면 옛 값이 나온다. **이 사유 줄의 델타와 방향은 어떤 검사도
//     안 본다**(사람이 읽는 기록이라 조용히 틀린 채 남는다).
//   발동률 근거: 해당 없다. 규칙을 스킬로 옮기지 않았다(글자만 바꿨다).
//   다음 델타는 27296 기준으로 계산한다.
const CEILING_BYTES = 27296;
// 밴드 폭. 선언을 여기 두는 이유 = 위 상한 테스트의 실패 메시지가 이 값을 참조하는데,
// 그 메시지는 콜백 실행마다 평가되므로 선언이 아래 있으면 러너의 실행 시점 규약에 의존하게 된다.
const BAND_BYTES = 64;

test(`주입되는 규칙 소스 전체(${INJECTED.join(" + ")})가 상한 ${CEILING_BYTES} bytes 이하 — 팽창은 one-in-one-out`, () => {
  const bytes = injectedBytes();
  assert.ok(
    bytes <= CEILING_BYTES,
    `주입 규칙 소스 합계(${INJECTED.join(" + ")}) = ${bytes} bytes > 상한 ${CEILING_BYTES}. ` +
    `규칙은 매 세션 상시 주입이라 팽창은 모든 세션 비용·규칙 희석을 부른다. ` +
    `다른 규칙을 줄여 상쇄하거나(one-in-one-out), 정말 필요하면 CEILING_BYTES를 ` +
    `**현재 정확한 크기로 다시 박고**(밴드 ${BAND_BYTES}B — 옛 "+2KB 이내" 안내는 밴드 도입으로 폐기) ` +
    `커밋에 "왜 코어가 커져야 하나 + 스킬 위임 검토" 근거를, 장부 주석에 델타 한 줄을 남겨라.`
  );
});

// v0.49.0 후속: 상한을 **정확한 크기 핀**으로 좁힌다(밴드 64B). 위쪽(팽창)만 막으면 구멍이 남는다 —
// 규칙을 지연 로드 스킬로 옮겨 코어를 줄이면 상한 테스트는 초록인데, 옮겨진 규칙은 그 스킬이 안 뜬
// 세션에서 조용히 꺼진다(v0.42.0 = 읽는 쪽만 있어 2,941회 중 0회 발동 · finish-check 저발동 실측
// = honclwd 제외 10개 프로젝트 중 1회). Codex 총면 상한이 주던 마찰의 대체물이다.
//
// 기존 커버리지(실측): 코어 본문을 앵커하는 테스트는 4개다 — rules-labels(SECTION_LABELS 14 +
// HOOK_SEEDS 11 + MISC_LABELS 6 + 스킬 포인터 5) · gate-round-marker(회차 쓰기 규칙) ·
// activate(주입 머리·지연로드 포인터) · unattended-loop-skill(질문 상한).
// ⚠ 코어 최상위 절은 **16개**, SECTION_LABELS는 14개다. 안 덮이는 절 = 파일 제목 ·
// `# User context · response language`(전수 확인). 단 16−14=2라는 뺄셈은 우연히 맞은 값이다 —
// SECTION_LABELS 14개 중 `게이트 판정 ↔ 멈춤`은 하위 절이고, `# Finish check` 절은 HOOK_SEEDS의
// "끝 점검"이 덮는다(두 어긋남이 상쇄). 커버리지는 **네 목록을 합쳐서** 봐야 한다(pr-reviewer low). 즉 "절 제목이 빠지면 잡는다"가 아니라 **"라벨에 오른
// 문자열이 빠지면 잡는다"**가 맞다. 어떤 테스트도 못 잡던 건 **절 안의 문단만 떼어 가는 경우**다.
//
// 폭 64의 근거: 이 저장소가 실제로 넣은 규칙 1건의 크기가 140·309·252바이트다(위 장부).
// 64는 그 최소치보다 작다. 초안의 512는 **틀렸다** — 당시 헤드룸 65 + 309 = 374 < 512라
// 막으려던 바로 그 사고(규칙 1건 이동)를 그대로 통과시켰다.
// 좋은 성질: 헤드룸은 **누적된다**. 50바이트씩 나눠 빼도 두세 번째 커밋에서 반드시 걸린다.
//
// 한계(정직 회계 — 축소해서 적지 않는다):
//  1. 64바이트 미만 조각 **한 번**은 못 잡는다(누적되면 잡힌다).
//  2. 코어에 다른 글자를 그만큼 더하면서 동시에 빼면 상쇄돼 못 잡는다.
//  3. **받는 쪽(src/skills·src/agents)에 크기 핀이 하나도 없다** → **새 규칙을 처음부터 코어가
//     아니라 스킬에만 넣으면 이 검사는 침묵한다**(코어 크기가 안 변하므로). 이게 진짜 사각이다.
//     (v0.64.1 에서 `src/rules/unattended-appendix.md` 는 이 목록에서 빠졌다 — 위 INJECTED 가
//     APPENDICES 에서 뽑아 함께 재기 시작했다. 스킬·에이전트는 여전히 사각이다.)
//     ("코어에 원본을 남긴 채 스킬에 복제"는 사고가 아니다 — 코어 원본이 계속 주입돼 규칙이 안 꺼진다.)
//  4. 비용: 재핀 후 헤드룸이 0이라 코어를 1바이트만 고쳐도 상수 재핀 + 장부 한 줄이 매번 따라온다.
//     의도된 마찰이다. 잦다는 이유로 **BAND_BYTES를 넓히지 말 것** — 512가 왜 틀렸는지가 위에 있다.
//  5. ⚠ **재핀하면 누적이 리셋된다.** 축소와 재핀을 같은 커밋에서 하면 기계는 영원히 초록이고,
//     그 재핀은 아래 실패 메시지가 직접 안내하는 정식 절차다. 즉 이 검사의 실체는 축소를 **막는**
//     자물쇠가 아니라 **상수 diff 한 줄을 사람 눈에 띄게 만드는 도장**이다. 위 '누적된다'는 성질도
//     재핀하지 않을 때만 성립한다(pr-reviewer low). 최종 방어선은 그 한 줄을 보는 리뷰어다.
test(`주입 규칙 소스가 상한에 붙어 있다(헤드룸 0~${BAND_BYTES}B) — 조용한 축소·느슨한 상한 차단`, () => {
  const headroom = CEILING_BYTES - injectedBytes();
  assert.ok(
    headroom >= 0 && headroom <= BAND_BYTES,
    `주입 규칙 소스(${INJECTED.join(" + ")}) 헤드룸 ${headroom} bytes (허용 0~${BAND_BYTES}). ` +
    `음수 = 상한 초과(위 테스트 메시지 참조). ` +
    `${BAND_BYTES} 초과 = 규칙 소스가 상한보다 그만큼 작다 — 둘 중 하나다. ` +
    `(a) 규칙을 줄였다 → CEILING_BYTES를 현재 정확한 크기로 다시 박고 장부에 사유 한 줄을 남겨라. ` +
    `(b) 규칙을 지연 로드 스킬로 옮겼다 → 그 스킬의 발동률 근거를 커밋 메시지에 남겨라. ` +
    `옮긴 규칙은 그 스킬이 안 뜬 세션에서 조용히 꺼진다. **그리고 (a)와 똑같이 상수를 다시 박아야** ` +
    `초록이 된다 — 근거만 적고 상수를 안 고치면 계속 빨간불이다. ` +
    `⚠ BAND_BYTES를 넓혀서 통과시키지 마라 — 그러면 이 검사의 존재 이유가 사라진다.`
  );
});

test(`주입 규칙 **파일별** 핀(밴드 0~${BAND_BYTES}B) — 파일 사이로 글을 옮겨 합계 핀을 피하는 길을 막는다`, () => {
  // 대상 목록이 곧 핀 목록이어야 한다(양방향). 부록을 새로 등록하면서 핀을 안 박으면 그 파일은
  // 파일별 축에 안 잡히고, 합계 핀만 재핀하면 통과해 버린다.
  assert.deepEqual(Object.keys(FILE_CEILINGS).sort(), [...INJECTED].sort(),
    "FILE_CEILINGS 와 주입 대상(INJECTED)이 어긋난다 — 새 부록을 등록했으면 파일별 핀도 함께 박아라.");
  // 두 장부가 따로 놀지 못하게 묶는다. 한쪽만 재핀하면 여기서 빨간불이다.
  const sum = Object.values(FILE_CEILINGS).reduce((a, b) => a + b, 0);
  assert.equal(sum, CEILING_BYTES,
    `파일별 핀 합계 ${sum} ≠ 합계 핀 ${CEILING_BYTES}. 재핀은 **두 장부를 함께** 고쳐야 한다 ` +
    "— 한쪽만 고치면 합계는 맞는데 파일별로는 틀리거나 그 반대가 된다.");
  for (const f of INJECTED) {
    const headroom = FILE_CEILINGS[f] - normBytes(join(RULES_DIR, f));
    assert.ok(headroom >= 0 && headroom <= BAND_BYTES,
      `${f} 헤드룸 ${headroom} bytes (허용 0~${BAND_BYTES}). ` +
      "음수 = 그 파일이 커졌다. 초과 = 그 파일이 작아졌다(다른 파일로 옮겼을 수 있다 — " +
      "합계 핀은 그 이동에 침묵한다. 코어는 **모든 세션**, 부록은 **무인 세션만** 비용이라 " +
      "옮기는 방향에 따라 상시 비용이 조용히 늘거나 준다). " +
      "둘 다 처방은 같다: FILE_CEILINGS 와 CEILING_BYTES 를 **함께** 현재 크기로 다시 박고 장부에 사유 한 줄.");
  }
});
